# CometChat UI Kit Implementation - React

This repository contains the implementation of the CometChat UI Kit using React.js, created as part of the CometChat Internship Task.

## 🚀 Project Overview
This project demonstrates a fully functional chat application generated via the CometChat UI Kit Builder and integrated into a React environment.

**Tech Stack:**
* React.js
* CometChat UI Kit
* HTML/CSS

## 📸 Screenshots & Demo

### 1. Login Screen
![Login Screen](./screenshots/login.jpg)
*(Replace with your login screen image)*

### 2. Chat Interface
![Chat Interface](./screenshots/chat.jpg)
*(Replace with your chat interface image)*

## 🛠️ Setup Instructions

1.  **Clone the repository:**
    ```bash
    git clone [YOUR_REPO_URL_HERE]
    ```

2.  **Install Dependencies:**
    ```bash
    npm install
    ```

3.  **Run the Application:**
    ```bash
    npm start
    ```
    Runs the app in the development mode. Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

## 📝 Findings & Observations
During the implementation, several observations were made regarding the UI Kit Builder flow and the generated code (deprecated dependencies, eslint warnings). A full report has been submitted via PDF.
